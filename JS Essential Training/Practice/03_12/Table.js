class Table {
  constructor(usage, chairs, color, place) {
    (this.usage = usage),
      (this.chairs = chairs),
      (this.color = color),
      (this.place = place);
  }
}

export default Table;
